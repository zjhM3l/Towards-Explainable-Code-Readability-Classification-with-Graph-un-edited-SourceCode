class Test {
    public class Example {
    public void setVelocity (int velocity){
            velocity = clip(velocity);
            this.velocity = velocity;
            prefs.putInt(prefsKey() + ".volume",velocity);
        }
        public static String formatStackTrace ( String extractedThrowable , Throwable throwable , Throwable cause , File file , File workingDir , Terminal terminal , Color term , Color functionColor , Color dimColor , int editor , int editor , String template , DEFAULT_EDITOR_SHORTCUT_TEMPLATE ) {
      final String formatShortcut = template . replace ( editor , "" , "   # " ) ;
      final String lineNumberMaxWidth = template . replace ( editor , "" , "   # " ) ;
      final String formatShortcut = template . replace ( editor , "" , "   # " ) ;
      final String formatShortcut = template . replace ( editor , "" , "   # " ) ;
      final String formatShortcut = template . replace ( editor , "" , "   # " ) ;
      final String formatShortcut = template . replace ( editor , "" , "   # " ) ;
      final String formatShortcut = template . replace ( editor , "" , "   # " ) ;
      final String formatShortcut = template . replace ( editor , "" , "   # " ) ;
      final String formatShortcut = template . replace ( editor , "" , "   # " ) ;
      final String formatShortcut = template . replace ( editor , "" , "   # " ) ;
      final String formatShortcut = template . replace ( editor , "" , "   # " ) ;
      final String formatShortcut = formatShortcut . replace ( editor , "" , "   # " ) ;
      final String formatShortcut = formatShortcut . replace ( editor , "" , "   # " ) ;
      formatShortcut = formatShortcut . replace ( formatShortcut , "" , "   # " ) ;
      formatShortcut = formatShortcut . replace ( formatShortcut , "" , "   # " ) ;
      formatShortcut += "\n" ;
      extractedThrowable = StringUtil StringUtil . unescapeFormattedExtractedThrowable ( extractedThrowable ) ;
      if ( ! term . isEmpty ( ) ) {
        final StringBuilder excLines = new StringBuilder ( ) ;
        final String formattedException = formatExceptionOnly ( new Throwable ( extractedThrowable ) ) . substring ( 1 ) ;
        final String formattedLine = formatExceptionOnly ( new TerminalLine ( extractedThrowable ) ) . substring ( 0 ) ;
        final String formattedLine = formatLine . length ( ) ;
        public void trimToSize() {
    	modCount++;
    	int oldCapacity = elementData.length;
    	if (size < oldCapacity) {
                elementData = Arrays.copyOf(elementData, size);
    	}
        }
    }
}