class Test {
    public class Example {
    private void erase() {
       		removeAll();
    		grid = Collections.synchronizedMap(new HashMap<String,GridObject>());
    		powergrid = new HashMap<String, GCompound>();
       	}
        public void mousePressed(MouseEvent e) {
            int x = translateInputX(e.getX());
            int y = translateInputY(e.getY());
            mouseDown = true;
            if (nearestGlyph != null) {
                repaint();
            }
        }
        public static String verifyId ( Context ctx , String param , String app ) {
      if ( app == null ) {
        throw new NullPointerException ( "Select an experiment using the --app parameter." ) ;
      }
      else if ( app . startsWith ( "dlgr-" ) ) {
        throw new IllegalArgumentException ( "UUID beginning with {
    }
    -..." . format ( app . substring ( 5 , 13 ) ) ) ;
      }
      return app ;
    }
    }
}